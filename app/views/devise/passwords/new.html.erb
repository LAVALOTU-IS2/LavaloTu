<%= stylesheet_link_tag 'registrations' %>
<div class="container data">
  <div class="logo">
    <%= link_to image_tag("logo.png", :alt => "Logo LavaloTu", :title => "Logo LavaloTu", :class=> "img-responsive"), root_path %>
  </div>
<h3>Recuperar contraseña</h3>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.email_field :email, autofocus: true, placeholder: "Correo"%>
  </div>

  <div class="actions">
    <%= f.submit "Enviar correo" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
